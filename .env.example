# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.

# ── Required for AI enrichment (optional — skipped if missing) ──────────────
# Get free key at: https://console.groq.com
GROQ_API_KEY=

# ── Required for Internshala + Naukri scraping (optional — skipped if missing)
# Get free key (500 credits/month) at: https://firecrawl.dev
FIRECRAWL_API_KEY=
